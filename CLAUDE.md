# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 저장소 성격

BNK금융지주『그룹 공동 생성형 AI 플랫폼 도입』입찰 **제안(presales) 작업 저장소**다. 현재는 분석 산출물(Markdown)만 있고 **코드·빌드 시스템·테스트·패키지 매니저가 없다.** 따라서 build/lint/test 명령이 존재하지 않는다 — 없는 명령을 만들어 실행하려 하지 말 것.

앞으로 만들 유일한 산출물은 **2026-09-09 제안설명회에서 수행 PM이 직접 시연할 통합 데모 HTML 1개 파일**이다. 실동작 백엔드는 범위 밖이며, mock 데이터로 화면 흐름만 재현한다.

**데드라인**: 제안서 접수 2026-09-07 15:00 / 제안설명회(데모 D-day) 2026-09-09. 제안서에 데모 스크린샷을 넣으려면 09-04까지 화면이 나와야 한다.

## 문서 지도

| 파일 | 역할 |
|---|---|
| `docs/2026-08-25-handoff-bnk-genai-platform-demo.md` | **작업 시작점.** 데모 스펙(3막 14화면), 기술 제약, 우선순위, 미해결 항목, 다음 액션 |
| `docs/BNK_그룹공동생성형AI플랫폼_RFP분석_20260824.md` (rev.3) | RFP 전체 분석 — 스코프 Top 10, Risk Register 20건, 솔루션 매핑, Issue Log, Follow-up 26건 |
| `docs/BNK_Effort재산정_KB앵커기준_20260824.md` (rev.3) | KB 앵커 delta 산정 (Base 277 MM / 컨틴전시 18% / Total 327 MM), 전제조건 9개 |

핸드오프 문서가 참조하는 `analysis/BNK_데이터영역_심화분석_20260824.md`, `06_PRICING_REFERENCE.md`, `07_PAST_PROJECTS.md` 등은 **이 저장소에 없다**(사내 AxOS vault 소재). 인용하되 읽으려 시도하지 말 것.

문서 규칙: 한국어 작성, YAML frontmatter(handoff 계열), 상단 개정 이력(rev.N), 파일명 `주제_YYYYMMDD.md` 또는 `YYYY-MM-DD-슬러그.md`. 문서를 개정할 때는 본문만 고치지 말고 **rev 이력 줄을 추가**하고, 어느 장이 갱신됐고 어느 장이 이전 rev 유효인지 명시하는 기존 패턴을 따를 것.

## 데모 HTML 하드 제약 (위반 시 발표장에서 깨진다)

- **완전 self-contained 단일 HTML.** 외부 CDN·폰트·이미지 호출 전면 금지. CSS·JS 전량 inline, 이미지는 data: URI 또는 인라인 SVG. 근거: 발표장 네트워크를 신뢰할 수 없다(RFP Ⅲ.13 — 발표자료 각사 자체 준비). 오프라인 더블클릭으로 열려야 한다.
- **localStorage 등 브라우저 스토리지 사용 금지.** 상태는 JS 메모리 변수로만.
- **1920×1080 판독성 기준.** 반응형은 부차. (2026-08-25 1280×720에서 상향 — 페이지 컨테이너 max-w 1760px)
- **Chrome/Edge에서 깨지지 않을 것** (RFP Ⅲ.1.13이 Chrome·Edge·Safari 호환 요구).
- **mock 데이터는 JS 객체로 파일 상단에 집약.** 시연 직전 문구 수정이 잦다.
- **실제 BNK 고객 데이터·실제 내규 문서 사용 절대 금지.** 전부 가상 데이터.
- **BNK 로고 이미지 도용 금지** — 중립적 워드마크/텍스트로 처리.
- 각 막(1~3막)을 **독립적으로 건너뛸 수 있게** 설계할 것. 발표 시간 배분이 미확정이라 7~10분 기준으로 만들되 잘라낼 수 있어야 한다.

## 도메인 구조 (화면 설계 시 필요한 최소 지식)

- RFP 기술요건표 **총 62개 = 필수 46 / 권고 16**. 이 중 발주처가 "상세제안 필요"로 표시한 **16개**(LSM-008/009, EDA-004/005/007, RAG-003/005, AGB-002/004/006/008, SEC-001/004/006/007, ONM-003)가 사실상 배점 집중 지점이며 **데모 화면 선정 기준**이다.
- 요건 ID 접두사: LSM(LLM 서빙·관리) / EDA(외부 계열사 데이터 연동) / RAG / AGB(에이전트·워크플로우 빌더) / SEC(보안, 9개 전량 필수) / ONM(운영·모니터링) / ADD.
- **데모의 클라이맥스는 Graph RAG 근거 그래프 시각화**다. RAG-007(Graph RAG, 필수)과 RAG-008(온톨로지 연계, 권고=가점)이 사내 자산 KT-Ontology로 동시 충족되는 유일한 명확 우위 지점이므로 시간을 여기에 가장 많이 쓴다.
- 백엔드 서사는 사내 자산 **AX Suite** 9개 모듈(Gateway·Model·Data(RAG)·Workflow·Tool(MCP)·Guardrail·Session·Observability·Experiments)이 LSM·RAG·AGB·SEC·ONM에 매핑되는 구조다.
- 인프라 전제: On-Premise BareMetal K8s, 공동존 상면, **11개 Namespace(10 계열사 + 공통 포털)**, 계열사 내부망과 격리.

**화면 우선순위** (시간 부족 시): P0 = 화면 3·4(Graph RAG 질의 + 근거 그래프), 9(승인 기반 DB 동적 라우팅 — Draft=익명화 개발DB / Approved=동의권원 확인 후 운영DB 복호화), 14(AI 거버넌스 포탈). P1 = 1·2·7·11. P2 = 5·6·8·10·12·13. 화면 번호와 대응 요건 ID는 핸드오프 §2 표 참조.

**현재 병목**: Graph RAG 화면의 근거가 될 **가상 규정 데이터셋**(규정 10~15건 + 규정 간 참조·개정 전후·전결권 관계). 이게 데모 품질을 좌우한다.

## 내용상 반드시 지킬 것

- **데모에서 보여준 기능은 확약이 된다.** RFP Ⅳ.6.7이 모호 표현을 "불가능"으로 간주하고 Ⅳ.4.1이 제안서에 계약서와 동일 효력을 부여한다. **구현 계획이 없는 기능을 화면으로 만들지 말 것.**
- **데이터 흐름을 그릴 때 정형=zero-copy(데이터 가상화) / 비정형=복제 적재 3분법을 지킬 것.** EDA-001의 "물리적 데이터 이동 없이"와 RAG 임베딩 복제 적재가 정면 충돌하는 미해소 이슈이므로, 화면마다 말이 달라지면 안 된다.
- 화면에서 보여준 것은 제안서 요건 조견표의 O 표기와 일치해야 한다.

## 범위 밖 (요청받지 않는 한 손대지 말 것)

제안서 본문 작성, 요건 62개 조견표 O/△/X 확정, 견적서·가격 산정, 발주처 RFI 발송, 컨소시엄·벤더 선정, 실동작 백엔드 구현(AX Suite·KT-Ontology 실연결).


# BNK GenAI Platform 시연 데모

KB GenAI 포털 mockup을 개조해 만드는 BNK금융지주 제안설명회(2026-09-09) 시연 데모.
스펙의 원천은 docs/2026-08-25-handoff-bnk-genai-platform-demo.md — 화면 구성이나
요건이 궁금하면 항상 이 파일을 먼저 확인할 것.

## 절대 규칙
- 오프라인 자립형: 외부 CDN·웹폰트·원격 이미지·외부 API 호출 금지.
  새 의존성을 추가할 때도 번들에 포함되는 것만.
- localStorage/sessionStorage/IndexedDB 사용 금지. 상태는 메모리(스토어)만.
- KB 브랜드 요소(로고·컬러·문자열·계열사명) 절대 잔존 금지. 발견 즉시 제거.
- 실제 금융사 데이터·실제 내규 문서 사용 금지. mock은 전부 가상 창작물.
- BNK 로고 이미지를 만들거나 삽입하지 않는다. 텍스트 워드마크로만 표기.
- 데모에 넣는 기능 = 제안 확약이다(RFP: 제안서는 계약서와 동일 효력).
  핸드오프 §2의 14화면 범위를 벗어나는 기능을 임의로 추가하지 않는다.

## Mock 데이터
- 모든 mock은 src/mock/ 아래 모듈로 중앙화. 컴포넌트에 하드코딩 금지.
  (시연 직전 문구 수정이 잦다 — 한 곳만 고치면 되게 유지)
- 테넌트 구조: 계열사 10개(가상 표기: 부산은행·경남은행 + 8개 계열사) + 그룹 공통,
  합계 11개 Namespace 구조가 화면에 드러나야 한다.

## 화면 ↔ 요건 추적
- 각 데모 화면 컴포넌트 상단에 대응 RFP 요건 ID를 주석으로 명시
  (예: // RFP: RAG-007, AGB-006① — Graph RAG 근거 그래프)
- 이유: 제안서 조견표와 상호 참조 검증에 사용

## 검증
- 완료 판정 전 반드시: ① `npm run build` 성공 ② dist를 네트워크 차단 상태로
  열어 전 화면 동작 ③ 1280×720 뷰포트에서 텍스트 판독 가능
- 개발 서버 확인만으로 "완료"라고 보고하지 말 것